Skip to content
Search or jump to…
Pull requests
Issues
Marketplace
Explore
 
@serikovkirill 
serikovkirill
/
pr_26_11
Public
1
00
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
pr_26_11/2
@serikovkirill
serikovkirill Create 2
Latest commit 8ca4eb8 7 minutes ago
 History
 1 contributor
133 lines (119 sloc)  2.9 KB
  
//Напишите функцию, которая позволяет изменить все элементы списка следующим
//образом: если значение data – четное, то разделить на 2 (2 * х); если нечетное, то
//умножить на 3 и вычесть 1 (3 * х - 1).
#include <iostream>
using namespace std;


struct Node {
    int val;
    Node* next;
    Node(int _val) : val(_val), next(nullptr) {}
};

struct list {
    Node* first;
    Node* last;

    list() : first(nullptr), last(nullptr) {}

    bool is_Empty() {
        return first == nullptr;
    }

    void push_back(int _val) {
        Node* p = new Node(_val);
        if (is_Empty()) {
            first = p;
            last = p;
            return;
        }
        last->next = p;
        last = p;
    }

    void print() {
        if (is_Empty()) return;
        Node* p = first;
        while (p) {
            cout << p->val << " ";
            p = p->next;
        }
        cout << endl;
    }

    Node* find(int _val) {
        Node* p = first;
        while (p && p->val != _val) p = p->next;
        return (p && p->val == _val) ? p : nullptr;
    }

    void remove_first() {
        if (is_Empty()) return;
        Node* p = first;
        first = p->next;
        delete p;
    }

    void remove_last() {
        if (is_Empty()) return;
        if (first == last) {
            remove_first();
            return;
        }
        Node* p = first;
        while (p->next != last) p = p->next;
        p->next = nullptr;
        delete last;
        last = p;
    }

    void remove(int _val) {
        if (is_Empty()) return;
        if (first->val == _val) {
            remove_first();
            return;
        }
        else if (last->val == _val) {
            remove_last();
            return;
        }
        Node* slow = first;
        Node* fast = first->next;
        while (fast && fast->val != _val) {
            fast = fast->next;
            slow = slow->next;
        }
        if (!fast) {
            cout << "This element does not exist" << endl;
            return;
        }
        slow->next = fast->next;
        delete fast;
    }

    Node* operator[] (const int index) {
        if (is_Empty()) return nullptr;
        Node* p = first;
        for (int i = 0; i < index; i++) {
            p = p->next;
            if (!p) return nullptr;
        }
        return p;
    }
    void reverse() {
        struct Node* p = first,
            * q = nullptr, * r;
        while (p != nullptr) {
            r = q;
            q = p;
            p = p->next;
            q->next = r;
        }
        first = q;
    }
};


int main()
{
    list l;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> i;
        if (i % 2 == 0) {
            l.push_back(i / 2);
        }
        else l.push_back(3 * i - 1);
    }
    l.print();
    l.reverse();
    l.print();
}
© 2021 GitHub, Inc.
Terms
Privacy
Security
Status
Docs
Contact GitHub
Pricing
API
Training
Blog
About
Loading complete